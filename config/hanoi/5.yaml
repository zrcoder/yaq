answer: |
  func solve(a, b, c func(), total int) {
      if total == 0 {
          return
      }
      solve(a, c, b, total-1)
      // move the only 1 disk from a to c
      a
      c
      solve(b, a, c, total-1)
  }

  solve A, B, C, 5
code: "func solve(a, b, c func(), total int) {\n    if total == 0 {\n        return\n
  \   }\n    solve(a, c, b, total-1)\n    // move the only 1 disk from a to c\n    \n
  \   \n    solve(b, a, c, total-1)\n}\n\nsolve A, B, C, 5\n"
disks: 5
hint: |
  complete the solve function
